plugins {
    id 'java-library'
    id 'kotlin'
    id 'java-gradle-plugin'
    id 'maven-publish'
}

java {
    sourceCompatibility = JavaVersion.VERSION_1_7
    targetCompatibility = JavaVersion.VERSION_1_7
}

gradlePlugin {
    plugins {
        create("myplugin") {
            id = "com.example.myplugin"
            implementationClass = "com.example.myplugin.AnalyticsPlugin"
        }
    }
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.example.myplugin'
            artifactId = 'library'
            version = '1.0'

            from components.java
        }
    }
    repositories {
        mavenLocal()
    }
}

java {
    withJavadocJar()
    withSourcesJar()
}

javadoc {
    if(JavaVersion.current().isJava9Compatible()) {
        options.addBooleanOption('html5', true)
    }
}
dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    implementation gradleApi()

    //引入asm库
    implementation 'org.ow2.asm:asm:9.2'
    implementation 'org.ow2.asm:asm-commons:7.0'
    implementation 'org.ow2.asm:asm-analysis:7.0'
    implementation 'org.ow2.asm:asm-util:7.0'
    implementation 'org.ow2.asm:asm-tree:7.0'
    compileOnly 'com.android.tools.build:gradle:4.1.1', {//这里注意需要保持版本一致，否则会报错
        //排除gradle自带的asm库
        exclude group:'org.ow2.asm'
    }
}